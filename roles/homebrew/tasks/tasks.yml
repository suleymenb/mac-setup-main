---
# Package lists live in group_vars/all.yml — do not add names here.

- name: Ensure Homebrew is installed (best-effort)
  shell: |
    set -euo pipefail
    if ! command -v brew >/dev/null 2>&1; then
      /bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"
    fi
  args:
    executable: /bin/bash
  changed_when: false

- name: Install brew formulae
  community.general.homebrew:
    name: "{{ brew_formulae }}"
    state: present
    update_homebrew: false

# The "_absent" lists may be empty or commented out entirely — default([])
# keeps that from blowing up with an undefined-variable error.
- name: Remove formulae that are no longer wanted
  community.general.homebrew:
    name: "{{ brew_formulae_absent | default([]) }}"
    state: absent
  when: (brew_formulae_absent | default([])) | length > 0

# --adopt makes Homebrew take ownership of an app that is already in
# /Applications instead of refusing with "It seems there is already an App at
# ...". That happens whenever an app was installed by hand, or when a previous
# cask install was interrupted after the app landed but before it was recorded.
- name: Install brew casks
  community.general.homebrew_cask:
    name: "{{ brew_casks }}"
    state: present
    install_options: adopt

- name: Remove casks that are no longer wanted
  community.general.homebrew_cask:
    name: "{{ item }}"
    state: absent
  loop: "{{ brew_casks_absent | default([]) }}"
  failed_when: false
